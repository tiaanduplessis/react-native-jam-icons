import React from "react";
import Svg, { Circle } from "react-native-svg";

const MoreAltF = props => (
  <Svg viewBox="0 0 16 64" {...props}>
    <Circle className="st0" cx={8} cy={56} r={8} />
    <Circle className="st0" cx={8} cy={8} r={8} />
    <Circle className="st0" cx={8} cy={32} r={8} />
  </Svg>
);

export default MoreAltF;
